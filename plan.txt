为了测试你的“工作流设计师”提示词，我为你准备了三组覆盖不同场景（硬件控制、数据处理、业务逻辑）的输入数据。

这些数据模拟了你之前关注的**机器人控制**和**独立开发**背景，可以很好地检验模型是否能正确处理 `dep` 依赖、变量传递以及 `try_` 探索型节点的生成。

---

### 测试用例 1：机器人感知与动作编排（硬件逻辑）
**场景描述**：测试模型在复杂硬件依赖下的拓扑排序能力。
* **输入指令**：读取激光雷达数据并检测障碍物，如果没有障碍物，则控制 6 轴机械臂执行折衣服动作，最后保存运行日志。
* **工具列表**：
    * `get_lidar_scan`, None, None, `points_cloud`
    * `run_arm_move`, `get_lidar_scan`, `motion_profile`, `move_status`
    * `save_log_file`, None, `content`, `file_path`

**预期输出逻辑**：
1. `get_lidar_scan` 获取点云。
2. **[关键点]** 模型需生成 `try_detect_obs` 节点分析点云。
3. **[关键点]** 模型需生成 `try_gen_motion` 节点规划折衣服动作。
4. `run_arm_move` 执行动作。
5. `save_log_file` 记录状态。

---

### 测试用例 2：电商数据分析与决策（业务逻辑）
**场景描述**：测试模型对 `input` 变量名一致性及 `prompt` 引用变量的理解。
* **输入指令**：从数据库提取上周库存数据，分析库存周转率，对积压商品生成促销文案，并更新到 Redis 缓存中。
* **工具列表**：
    * `get_db_inventory`, None, `query_sql`, `raw_data`
    * `set_redis_kv`, None, `key_value_pair`, `op_result`

**预期输出逻辑**：
1. `get_db_inventory` 获取原始数据。
2. 生成 `try_calc_turnover` 计算周转率。
3. 生成 `try_gen_promo` 根据分析结果写文案。
4. `set_redis_kv` 将文案存入缓存。

---

### 测试用例 3：闭环控制异常处理（算法逻辑）
**场景描述**：测试模型处理“多步依赖”和“条件判断转化为 Prompt”的能力。
* **输入指令**：获取 AS5600 编码器角度和电机电流，计算 FOC 电压矢量，若电流超过安全阈值，则生成报警摘要并关闭驱动器。
* **工具列表**：
    * `get_encoder_angle`, None, None, `angle_rad`
    * `get_current_sense`, None, None, `iq_ia`
    * `run_foc_update`, `get_encoder_angle`, `target_v`, `pwm_duty`
    * `set_driver_state`, None, `enable_bool`, `status`

**预期输出逻辑**：
1. 分别获取角度和电流。
2. 生成 `try_check_safety` 节点（引用电流值判断是否过流）。
3. 生成 `try_calc_foc` 节点计算矢量。
4. `run_foc_update` 更新硬件。
5. `set_driver_state` 根据安全检查结果操作。

---

太棒了！从日志看，你的 `WorkflowArchitectNode` 已经完全掌握了**“语义参数+语义注释”**的精髓，生成的 DAG 结构非常清晰，`try_filter` 的外包逻辑也处理得恰到好处。

为了深度测试这个架构的鲁棒性（尤其是测试 **ToolSelector 的精准度**、**DAG 的拓扑依赖**以及**动态节点的生成能力**），我为你准备了三个不同维度的指令：

---

### 测试指令 1：多步骤依赖与数据过滤
**目标：** 测试 `dep` 拓扑排序和 `try_` 节点的逻辑链条。
> **指令：** "先查看当前系统的运行时间（uptime），然后获取所有进程列表，最后帮我分析一下：在系统运行了这么久的情况下，当前哪个进程的资源占用最不合理？"

* **预期表现：**
    * `Selector`: 选中 `uptime` 和 `ps`。
    * `Architect`: 
        1. `[uptime] -> out: v1`
        2. `[ps] -> out: v2`
        3. `[try_analyze] -> dep: [1, 2] | prompt: "结合系统运行时间 {v1} 分析 {v2} 中的异常进程" | out: r1`

---

### 测试指令 2：磁盘空间预警（包含常量输入）
**目标：** 测试 `input` 传递常量（如目录路径）以及对特定工具的调用。
> **指令：** "检查一下 /home 目录的磁盘占用情况，如果空间不足，请列出该目录下最大的三个文件夹。"

* **预期表现：**
    * `Selector`: 选中 `du` 或 `df`。
    * `Architect`: 
        1. `[du] -> input: "/home"` (测试它是否能正确把路径作为 Literal 传入)。
        2. `[try_filter] -> dep: 1 | prompt: "从 {v1} 中找出占用空间最大的三个文件夹" | out: r1`

---

### 测试指令 3：网络配置检索（复杂意图识别）
**目标：** 测试当用户指令模糊时，模型是否会选择正确的工具，并利用注释解释意图。
> **指令：** "我的网络好像有点慢，帮我看看当前的网卡状态和路由表配置。"

* **预期表现：**
    * `Selector`: 选中 `ip`。
    * `Architect`: 可能会生成两步调用同一个工具但 `input` 不同：
        1. `[ip] -> input: "addr"` # 查看网卡地址
        2. `[ip] -> input: "route"` # 查看路由表
        3. `[try_summary] -> dep: [1, 2] | prompt: "诊断网络配置是否异常"`

---

### 调试小建议：
你可以直接在 `if __name__ == "__main__":` 的 `initial_input` 里轮换这三个字符串进行测试。

**观察重点：**
1.  **Linter 是否报错：** 尤其是在指令 1 这种双重依赖（Step 3 依赖 Step 1 和 2）的情况下，看 `dep` 字段是否写成了 `[1, 2]`（如果你的正则支持）或者分两次依赖。
2.  **注释的质量：** 看看 `# comment` 是否真的解释了为什么要传这些参数。
3.  **Token 消耗：** 随着指令变复杂，看 `architect` 的输入 Token 是否因为 `Tool List` 的增加而显著增长。

你想先跑哪一个？或者需要我帮你针对“多重依赖”优化一下 `validate_workflow_dag` 的正则（比如支持 `dep: 1, 2` 这种格式）？

