# CutinAgent 环境配置说明

本文档涵盖从零开始部署 CutinAgent 的完整流程，包括系统依赖安装、Docker + Ollama 配置、模型定制、以及参数调优指南。已配置好环境的用户可作为自查清单使用。

---

## 环境要求

| 组件 | 要求 |
|------|------|
| 操作系统 | Ubuntu 24.04 LTS (x86_64) |
| GPU | NVIDIA RTX 3060 6GB（或同等显存） |
| Docker | Docker Engine + Docker Compose v2 插件 |
| NVIDIA 组件 | NVIDIA 驱动 + NVIDIA Container Toolkit |
| Python | 3.10+ |

---

## 一、安装系统依赖

以下从一台刚装好 Ubuntu 24.04 的机器开始，逐步完成所有依赖安装。已装好的步骤可跳过。

### 1. NVIDIA 驱动

大多数 Ubuntu 24.04 系统已自带驱动，先检查：

```bash
nvidia-smi
```

如果命令存在且显示 GPU 信息（如 `NVIDIA GeForce RTX 3060 ...`），驱动已装好，跳到下一步。

如果提示 `command not found`：

```bash
sudo apt update
sudo apt install nvidia-driver-550 -y
sudo reboot
# 重启后验证
nvidia-smi
```

### 2. Docker

```bash
# 官方一键安装脚本
curl -fsSL https://get.docker.com | sudo bash

# 将当前用户加入 docker 组，避免每次敲 sudo
sudo usermod -aG docker $USER
```

> **重要**：执行 `usermod -aG docker` 后，需**注销重新登录**（或执行 `newgrp docker`）才能生效。用 `docker ps` 验证——不报权限错误即成功。

### 3. Docker Compose 插件

```bash
sudo apt install docker-compose-plugin -y
docker compose version   # 验证
```

### 4. NVIDIA Container Toolkit

> **这是最容易卡住的步骤。** 没有它，Docker 容器内看不到 GPU，Ollama 只能用纯 CPU 推理——4B 模型每秒一个字，基本没法用。

```bash
# 添加 NVIDIA 官方仓库
curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey \
    | sudo gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg

curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list \
    | sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' \
    | sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list

sudo apt update
sudo apt install nvidia-container-toolkit -y

# 重启 Docker 使配置生效
sudo systemctl restart docker

# 验证：Docker 能否看到 GPU
docker run --rm --gpus all nvidia/cuda:12.4.0-base-ubuntu22.04 nvidia-smi
```

最后一条命令输出 GPU 信息即配置成功。

#### 国内网络问题

以上 `curl` / `apt` / `docker pull` 命令可能因网络问题失败。遇到超时或 `Connection refused` 时：

- **Docker 镜像加速**：编辑 `/etc/docker/daemon.json`，添加国内镜像源（如阿里云、中科大等），然后 `sudo systemctl restart docker`
- **apt 源**：将 `/etc/apt/sources.list` 替换为清华或阿里云镜像源
- **Ollama 模型拉取**：后续模型拉取走 Ollama 官方仓库，网络慢时需要代理或等待

> 具体镜像地址随时间变化，请自行搜索 "Ubuntu 24.04 国内源" 和 "Docker 国内镜像加速" 获取最新配置。

---

## 二、部署项目

### 一键自动配置（推荐）

系统依赖装好后，剩余步骤交给自动脚本：

```bash
git clone https://github.com/GTLordLL/CutinAgent.git
cd CutinAgent
bash setup.sh
```

`setup.sh` 自动完成：环境自检 → 启动 Ollama 容器 → 拉取基础模型 → 创建定制模型 → 配置 Python 虚拟环境。全程幂等，已完成的步骤跳过，失败自动清理中间产物。

### 手动配置

#### 1. 启动 Ollama 容器

项目根目录提供了 `docker-compose.yml`：

```bash
docker compose up -d
curl http://localhost:11434/api/tags   # 验证
```

#### 2. 拉取模型并创建定制版本

本项目 Prompt 较长（含完整 SOP 计划 + 历史步骤），默认 2K 上下文不够用，需扩展至 8K：

```bash
# 拉取基础模型（Q4_K_M 量化, 约 2.5 GB）
docker exec cutin-ollama ollama pull qwen3:4b-instruct

# 创建定制模型
docker exec cutin-ollama ollama create qwen3:4b-instruct_8k -f - << 'MODELFILE'
FROM qwen3:4b-instruct
TEMPLATE "{{ .Prompt }}"
PARAMETER num_ctx 8192
PARAMETER num_gpu 40
PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
MODELFILE

# 验证
docker exec cutin-ollama ollama list | grep qwen3:4b-instruct_8k
```

参数说明见下方 **模型参数调优**。

#### 3. 安装 Python 依赖

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

> **远程 Ollama**：如果 Ollama 部署在其他机器，设置 `export OLLAMA_BASE_URL=http://<your-host>:11434`。

---

## 三、模型参数调优

定制模型 `qwen3:4b-instruct_8k` 的两个关键参数需要根据你的硬件调整：

### `num_gpu` — GPU 层数

决定多少层模型参数加载到 GPU。**值越大，推理越快，但显存占用越高。**

- qwen3 4B 共约 36 层，设为 **40**（默认）可保证全部层加载到 GPU
- 在 RTX 3060 6GB 上实测：全部层上 GPU 仅占约 4.2 GB 显存，速度 **~45 tokens/s**
- 如果你的 GPU 显存较小（如 4GB），适当减小此值，让部分层回退 CPU（速度下降但能跑）
- 如果你的 GPU 显存充裕（如 12GB+），保持 40 或更高即可

查看实际显存占用：

```bash
docker exec cutin-ollama nvidia-smi
```

### `num_ctx` — 上下文窗口

决定模型一次能"看到"多少 token。**值越大，能处理更长的对话和 Prompt，但显存占用也越高。**

- 设为 **8192**（默认），满足本项目完整 SOP 计划 + 多轮历史的需求
- 可调整为 4096（省显存）或 16384（更多历史），按需选择
- 注意：上下文窗口翻倍 ≈ 显存占用显著增加

### 调整方法

修改上述 Modelfile 中的参数值，重新创建模型：

```bash
docker exec cutin-ollama ollama create qwen3:4b-instruct_8k -f - << 'MODELFILE'
FROM qwen3:4b-instruct
TEMPLATE "{{ .Prompt }}"
PARAMETER num_ctx 8192
PARAMETER num_gpu 40
PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
MODELFILE
```

重新创建会覆盖同名模型，无需删除旧的。

---

## 四、运行

```bash
source .venv/bin/activate
python main.py
```

修改 `main.py` 顶部的 `ACTIVE_CASE` 可切换测试场景。输入自然语言指令示例：

- `检查一下docker服务的运行状态`
- `我怀疑/var/log目录磁盘占用太大`
- `对当前系统做一次全面的健康检查`

---

## 五、卸载

```bash
# 停止并删除 Ollama 容器（保留模型数据卷）
docker compose down

# 停止并删除容器 + 模型数据卷（彻底清除）
docker compose down -v
```
