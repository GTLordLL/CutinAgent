帮我写“工具执行专家”的英文提示词吧，
1.使用脚本将WORKFLOW_DAG对应的节点提取，并将工具id，input，注释提取出来；
2.若input中有前面节点的输出变量，则将数据取出；
3.再将工具功能描述和tool_schema取出；
这三种是输入数据。
基于“工具id”，“工具功能描述”，“input“，”变量”，“tool_schema”，编写并输出，符合“注释”目的和工具需要的tool_schema。

工具类型,Schema 示例 (In Context),Executor 处理逻辑
Shell,sh:ls(args:str),"subprocess.run(f""ls {args}"")"
Python,py:get_weather(city:str),globals()[func_name](**args)
MCP,mcp:resource/read(uri:str),调用 MCP SDK Client
Agent,try_agent(prompt:str),递归调用 LLM

