二、代码中存在但 CLAUDE.md 未提及的设计要点
  
  以下列出的是实际影响代码行为、但文档中未说明的点：

  1. 无图级最大循环守卫（潜在风险）

  graph/Builder.py 中 3 节点循环没有 max_rounds 上限。如果 Scheduler 持续输出
  ONGOING，图会无限循环 → LLM 无限调用。代码中 current_round 只递增但从不被检查。

  2. VariableStore 永不自动清除

  data_nodes/VariableStore.py 的 _store 是模块级 dict，会话期间无限增长。同一 SOP
  执行两次会产生 VAR_xxx、VAR_xxx_2、VAR_xxx_3... 旧数据不会过期。clear()
  存在但只被手动调用。

  3. "并行"工具执行实际是顺序的

  data_nodes/ToolExecutor.py 使用同步 for tc in tool_calls: 循环。PARALLEL 步骤类型和
  current_tool_calls 列表存在，但没有使用 asyncio 或 ThreadPoolExecutor。

  4. 所有 Formatter 节点共享同一个 LLM 实例

  三个 LLM 节点（UserCoordinator、Compactor、Scheduler）的 Formatter 全部使用
  config/model_config.json 中同一个 all_formatter 配置（temp 0.0, top_p
  0.1）。这在配置文件中可见，但 CLAUDE.md 未明确说明三个节点共享 Formatter
  实例这一设计决策。

  5. Compactor 调用无异常保护

  main.py 中 SOP 图执行有 try/except，但 Compactor 调用（compactor_fn(state)）没有。如果
  Compactor 的 LLM 调用失败，整个 REPL 会崩溃。

  6. monitor_token.py 未被使用

  写在 utils/ 中但没有任何节点调用 monitor_token_usage()。是遗留模块。

  7. ProgressUpdater 之后始终路由回 Scheduler

  即使 task_status 是 ERROR 或 INTERRUPT，图边仍然是 progress_updater → 
  sop_execution_scheduler。终止是由 Scheduler 再次被调用后才能输出终端状态，然后
  route_after_scheduler 才路由到 END。这意味着每个终止状态都会多调用一次
  Scheduler（LLM）。